// For Payload CMS v2+, we'll handle workflow logic in collection hooks instead of plugins
// This file is kept for future plugin development
 
export const workflowPlugin = {
  // Plugin configuration for future use
  // In Payload CMS v2+, plugins work differently
  // We'll handle workflow logic in collection hooks
}; 